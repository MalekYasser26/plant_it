import 'package:flutter/material.dart';

class CustStagTile extends StatelessWidget {
  const CustStagTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: const BoxDecoration(
        borderRadius: BorderRadius.all(Radius.circular(20)),
        color: Colors.purple,
      ),
      child: const Center(child: Text("Ssssssss")),
    );
  }
}
